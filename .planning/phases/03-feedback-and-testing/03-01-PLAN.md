---
phase: 03-feedback-and-testing
plan: "01"
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - src/components/FeedbackDialog.tsx
  - src/lib/submitFeedback.ts
  - src/components/Header.tsx
  - extension/app/assets/index.js
autonomous: true
requirements:
  - FBK-01
  - FBK-02
must_haves:
  truths:
    - "Feedback button is visible in the extension side panel header at all times (detecting, ready, no-show, needs-episode)"
    - "Clicking the feedback button opens a dialog with a textarea"
    - "Submitting feedback POSTs the correct payload (text, showTitle, platform, season, episode) to log-feedback"
    - "A success toast appears after submit and the dialog closes"
    - "Extension bundle is rebuilt so the feedback button appears in Chrome"
  artifacts:
    - path: "src/components/FeedbackDialog.tsx"
      provides: "Dialog trigger button + submission form + fetch call"
      exports: ["FeedbackDialog"]
    - path: "src/lib/submitFeedback.ts"
      provides: "submitFeedback(text, meta) async function"
      exports: ["submitFeedback"]
    - path: "src/components/Header.tsx"
      provides: "FeedbackDialog wired into right icon row"
      contains: "FeedbackDialog"
    - path: "extension/app/assets/index.js"
      provides: "Rebuilt extension bundle containing FeedbackDialog"
  key_links:
    - from: "src/components/Header.tsx"
      to: "src/components/FeedbackDialog.tsx"
      via: "import + JSX render"
      pattern: "FeedbackDialog"
    - from: "src/components/FeedbackDialog.tsx"
      to: "src/lib/submitFeedback.ts"
      via: "import + call on form submit"
      pattern: "submitFeedback"
    - from: "src/lib/submitFeedback.ts"
      to: "supabase/functions/log-feedback"
      via: "fetch POST"
      pattern: "log-feedback"
---

<objective>
Build the FeedbackDialog component, wire it into Header, and rebuild the extension bundle so users can submit feedback from within the side panel.

Purpose: Satisfies FBK-01 (entry point accessible at all times) and the client-side half of FBK-02 (correct payload submitted). The feedback button sits beside the history icon in the header and is visible in every init phase.
Output: FeedbackDialog.tsx, submitFeedback.ts, updated Header.tsx, rebuilt extension bundle.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-feedback-and-testing/03-RESEARCH.md

@src/components/Header.tsx
@src/lib/types.ts
</context>

<interfaces>
<!-- Existing contracts the executor must use — do not redefine. -->

From src/lib/types.ts:
```typescript
export interface SessionMeta {
  sessionId: string;
  showId?: number;
  showTitle: string;
  platform: string;
  season: string;
  episode: string;
  context: string;
  lastMessageAt: number;
  messageCount: number;
  confirmed?: boolean;
}
```

From src/components/Header.tsx (existing props — do NOT change the interface):
```typescript
interface HeaderProps {
  statusBadgeProps?: StatusBadgePassthroughProps;
  onOpenHistory?: () => void;
  sessionCount?: number;
  onRefresh?: () => void;
}
// StatusBadgePassthroughProps includes: meta: SessionMeta | null
```

History button pattern (replicate for feedback button):
```tsx
<Button
  variant="ghost"
  size="sm"
  onClick={onOpenHistory}
  className="h-7 w-7 p-0 text-muted-foreground hover:text-foreground relative"
  aria-label="Chat history"
>
  <History className="w-4 h-4" />
</Button>
```

Environment variables (already set in .env.local):
```
VITE_SUPABASE_URL
VITE_SUPABASE_PUBLISHABLE_KEY
```

Existing toast pattern (from useChat.ts / audit-answer wiring):
```typescript
import { toast } from 'sonner';
toast('Safety edit applied');   // simple string toast
toast.error('...');             // error variant
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create submitFeedback utility and FeedbackDialog component</name>
  <files>src/lib/submitFeedback.ts, src/components/FeedbackDialog.tsx</files>
  <behavior>
    - submitFeedback('text', meta) POSTs to `${VITE_SUPABASE_URL}/functions/v1/log-feedback` with Authorization header
    - POST body contains: { text, showTitle, platform, season, episode } (null when meta is null)
    - FeedbackDialog renders a ghost icon button (h-7 w-7 p-0, MessageSquare icon, aria-label="Send feedback")
    - Clicking the trigger button opens a Radix Dialog
    - Dialog contains a textarea (required) and a Send button
    - Send button is disabled when textarea is empty or while submitting
    - On successful submit: calls submitFeedback, shows sonner toast "Thanks for the feedback!", closes dialog, clears textarea
    - On error: shows toast.error("Couldn't send feedback — try again")
  </behavior>
  <action>
**Step 1 — Create src/lib/submitFeedback.ts:**

Export a single async function `submitFeedback(text: string, meta: SessionMeta | null): Promise<void>`.

Use `import.meta.env.VITE_SUPABASE_URL` and `import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY` for the endpoint and auth header. Mirror the fetch pattern from `useEpisodeRecap.ts` / `useInitFlow.ts`. POST body: `{ text: text.trim().slice(0, 2000), showTitle: meta?.showTitle ?? null, platform: meta?.platform ?? null, season: meta?.season ?? null, episode: meta?.episode ?? null }`. Throw on non-ok response so the caller can catch.

**Step 2 — Create src/components/FeedbackDialog.tsx:**

Props: `{ meta: SessionMeta | null }`.

Use `@radix-ui/react-dialog` (Dialog, DialogContent, DialogHeader, DialogTitle — already installed). Import `MessageSquare` from `lucide-react`. Import `Button` from `@/components/ui/button`. Import `toast` from `sonner`. Import `submitFeedback` from `@/lib/submitFeedback`.

State: `open: boolean`, `text: string`, `isSubmitting: boolean`.

Trigger: ghost icon Button with `aria-label="Send feedback"` and `MessageSquare` icon. Use identical styling to the history button: `className="h-7 w-7 p-0 text-muted-foreground hover:text-foreground"`.

Dialog width: `className="w-[320px] sm:max-w-[320px]"`. DialogTitle: "Send Feedback". Textarea: `className="w-full rounded-md border border-border bg-background px-3 py-2 text-sm resize-none h-24 mt-2"`, placeholder "What's on your mind?". Send Button below textarea, `disabled={!text.trim() || isSubmitting}`.

handleSubmit: set isSubmitting=true, call submitFeedback, on success toast + setOpen(false) + setText(''), on error toast.error, always setIsSubmitting(false).

Run tests after creating both files: `npx vitest run src/test/FeedbackDialog.test.tsx src/test/submitFeedback.test.ts --reporter=verbose`
  </action>
  <verify>
    <automated>cd /Users/abdulsaboorshaikh/Downloads/spoiler-shield && npx vitest run src/test/FeedbackDialog.test.tsx src/test/submitFeedback.test.ts --reporter=verbose</automated>
  </verify>
  <done>All tests in FeedbackDialog.test.tsx and submitFeedback.test.ts pass (GREEN). submitFeedback.ts and FeedbackDialog.tsx exist with correct exports.</done>
</task>

<task type="auto">
  <name>Task 2: Wire FeedbackDialog into Header and rebuild extension bundle</name>
  <files>src/components/Header.tsx, extension/app/assets/index.js</files>
  <action>
**Step 1 — Update Header.tsx:**

Add `import { FeedbackDialog } from '@/components/FeedbackDialog';` at the top.

In the side-panel branch only (inside `if (isSidePanel)`), add `<FeedbackDialog meta={statusBadgeProps?.meta ?? null} />` to the right icon row flex div, after `{statusBadgeProps && <StatusBadge {...statusBadgeProps} />}` and before the history button. The flex row already has `gap-1.5` spacing — no layout changes needed.

Do NOT add FeedbackDialog to the web-app header branch. The feedback button is side-panel only (the web-app header has no history button either — it's a simpler layout).

**Step 2 — Rebuild extension bundle:**

```bash
cd /Users/abdulsaboorshaikh/Downloads/spoiler-shield && BUILD_TARGET=extension npm run build
```

Confirm `extension/app/assets/index.js` is updated (check file mtime or that it contains "FeedbackDialog" or "Send feedback").

**Step 3 — Run full test suite:**

```bash
npx vitest run --reporter=verbose
```
  </action>
  <verify>
    <automated>cd /Users/abdulsaboorshaikh/Downloads/spoiler-shield && npx vitest run --reporter=verbose && grep -l "FeedbackDialog\|Send feedback" extension/app/assets/index.js</automated>
  </verify>
  <done>Header.tsx imports and renders FeedbackDialog in the side-panel branch. Extension bundle rebuilt and contains "Send feedback" aria-label text. Full vitest suite passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests green
2. `grep "FeedbackDialog" src/components/Header.tsx` — confirms import and render
3. `grep -c "Send feedback" extension/app/assets/index.js` — confirms bundle contains the aria-label
4. `npm run dev` starts without TypeScript errors
</verification>

<success_criteria>
- FeedbackDialog.tsx exported from src/components/ with correct prop interface
- submitFeedback.ts exported from src/lib/ — POSTs correct payload shape
- Header.tsx side-panel branch renders FeedbackDialog with meta from statusBadgeProps
- Extension bundle rebuilt — feedback button present in Chrome extension without reloading src
- All vitest tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-feedback-and-testing/03-01-SUMMARY.md`
</output>
