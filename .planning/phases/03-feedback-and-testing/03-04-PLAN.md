---
phase: 03-feedback-and-testing
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified: []
autonomous: false
requirements:
  - FBK-01
  - FBK-02
  - FBK-03
must_haves:
  truths:
    - "Feedback button visible in Chrome extension side panel (all phases)"
    - "Submitted feedback row appears in Supabase Studio feedback table"
    - "Admin dashboard /admin shows the submitted row after entering the password"
  artifacts: []
  key_links:
    - from: "FeedbackDialog (Chrome extension)"
      to: "log-feedback Edge Function"
      via: "fetch POST with Authorization header"
      pattern: "n/a — verified manually"
    - from: "log-feedback Edge Function"
      to: "public.feedback table"
      via: "supabase.from('feedback').insert()"
      pattern: "n/a — verified via Supabase Studio"
    - from: "landing/app/admin/page.tsx"
      to: "public.feedback REST endpoint"
      via: "fetch /rest/v1/feedback"
      pattern: "n/a — verified by navigating /admin"
---

<objective>
End-to-end verification of the full feedback pipeline: button visible in extension → submit → row in DB → visible in admin dashboard.

Purpose: Confirms all three FBK requirements work as a connected system in the live Chrome extension and deployed Supabase backend — not just individually. This checkpoint cannot be automated because it requires a live Chrome session with the extension loaded.
Output: All three success criteria from the Phase 3 ROADMAP confirmed.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-feedback-and-testing/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm smoke-test readiness</name>
  <files></files>
  <action>
Run the complete vitest suite to confirm all automated tests are green before the human verify step:

```bash
cd /Users/abdulsaboorshaikh/Downloads/spoiler-shield && npx vitest run --reporter=verbose
```

All tests must pass. If any test is failing, stop and fix it before proceeding.

Also confirm the extension bundle is up to date by checking its mtime vs the latest src/ changes:

```bash
ls -la /Users/abdulsaboorshaikh/Downloads/spoiler-shield/extension/app/assets/index.js
```

If the bundle is older than `src/components/FeedbackDialog.tsx`, rebuild:

```bash
cd /Users/abdulsaboorshaikh/Downloads/spoiler-shield && BUILD_TARGET=extension npm run build
```
  </action>
  <verify>
    <automated>cd /Users/abdulsaboorshaikh/Downloads/spoiler-shield && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All vitest tests pass. Extension bundle is current (newer than FeedbackDialog.tsx).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - FeedbackDialog button in extension Header (Plan 03-01)
    - log-feedback Edge Function + feedback Supabase table (Plan 03-02)
    - /admin dashboard with password gate in landing app (Plan 03-03)
  </what-built>
  <how-to-verify>
**Verify FBK-01 — Feedback button accessible at all times:**

1. Open Chrome with the Veil extension loaded (reload extension if needed from chrome://extensions)
2. Navigate to a Crunchyroll episode page
3. Confirm the MessageSquare (speech bubble) icon button appears in the extension header, to the right of the episode badge and left of (or beside) the history icon
4. Navigate to the Crunchyroll home page (no episode) — confirm the feedback button is still visible
5. Click the feedback button — confirm a dialog opens with a textarea and a "Send" button

**Verify FBK-02 — Submission stored in Supabase:**

6. Type "E2E test submission" in the textarea and click Send
7. Confirm a "Thanks for the feedback!" toast appears and the dialog closes
8. Open Supabase Studio: https://supabase.com/dashboard/project/dbileyqtnisyqzgwwive/editor
9. Run: `select * from feedback order by created_at desc limit 5;`
10. Confirm a row appears with text "E2E test submission" and the correct show_title and platform

**Verify FBK-03 — Admin dashboard shows the submission:**

11. Start the landing dev server: `cd landing && npm run dev`
12. Navigate to http://localhost:3000/admin
13. Confirm the password gate (input + Unlock button) is visible
14. Enter `veil-admin-2026` and click Unlock
15. Confirm the feedback table appears showing the row submitted in step 6
16. Reload the page — confirm the table is still visible (auth persisted in localStorage)
17. Click Sign out — confirm the password gate returns
  </how-to-verify>
  <resume-signal>Type "approved" if all 17 steps pass. Describe any failures so they can be fixed before phase close.</resume-signal>
</task>

</tasks>

<verification>
All verification is human-driven per the checkpoint above. Automated test suite (Task 1) must be green before the checkpoint is attempted.
</verification>

<success_criteria>
- "approved" from user after completing all 17 verification steps
- Phase 3 ROADMAP success criteria confirmed:
  1. Feedback entry point visible in extension at all times (FBK-01)
  2. Submitted feedback stored in Supabase with timestamp + metadata (FBK-02)
  3. Admin dashboard displays submissions, protected by password (FBK-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-feedback-and-testing/03-04-SUMMARY.md`
</output>
