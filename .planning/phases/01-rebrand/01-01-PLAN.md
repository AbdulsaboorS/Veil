---
phase: 01-rebrand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/manifest.json
  - extension/content.js
  - extension/sidepanel.js
  - extension/app/index.html
autonomous: true
requirements:
  - REB-02
  - REB-04
  - REB-05
must_haves:
  truths:
    - "Extension manifest name shows 'Veil' on the Chrome extensions management page"
    - "chrome.storage.local uses veil_show_info and veil_context keys (no spoilershield_ keys)"
    - "postMessage types sent by sidepanel.js use VEIL_* names (no SPOILERSHIELD_* strings)"
    - "sidepanel.js responds to VEIL_REQUEST_SHOW_INFO, VEIL_REQUEST_CONTEXT, VEIL_REQUEST_REDETECT"
    - "No [SpoilerShield] console.log output from extension scripts"
  artifacts:
    - path: "extension/manifest.json"
      provides: "Extension identity"
      contains: "\"name\": \"Veil\""
    - path: "extension/content.js"
      provides: "Show detection, chrome.storage writes"
      contains: "veil_show_info"
    - path: "extension/sidepanel.js"
      provides: "Bridge between storage and React app"
      contains: "VEIL_SHOW_INFO"
    - path: "extension/app/index.html"
      provides: "HTML shell title"
      contains: "Veil"
  key_links:
    - from: "extension/sidepanel.js"
      to: "extension/content.js"
      via: "chrome.storage.local key veil_show_info"
      pattern: "veil_show_info"
    - from: "extension/sidepanel.js"
      to: "src/hooks/useInitFlow.ts"
      via: "window.postMessage VEIL_SHOW_INFO"
      pattern: "VEIL_SHOW_INFO"
---

<objective>
Rename all extension-layer artifacts from SpoilerShield to Veil: the manifest identity, chrome.storage.local keys used between content.js and sidepanel.js, all window.postMessage type strings sent/received by sidepanel.js, and console.log prefixes.

Purpose: The Chrome extension management page, chrome.storage, and the postMessage protocol all need consistent Veil naming before the React app layer can be updated in Plan 02 (which renames the receiving side of those postMessage types).

Output: manifest.json with Veil name/description, content.js writing veil_* storage keys, sidepanel.js sending VEIL_* message types and reading veil_* storage keys, extension/app/index.html with updated title.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rebrand/01-CONTEXT.md

<interfaces>
<!-- chrome.storage.local keys: current → target -->
<!-- content.js writes (lines 295, 510): -->
<!--   spoilershield_show_info → veil_show_info -->
<!--   spoilershield_context   → veil_context   -->
<!-- sidepanel.js reads/watches: -->
<!--   spoilershield_show_info → veil_show_info -->
<!--   spoilershield_context   → veil_context   -->

<!-- postMessage types: current → target -->
<!-- sidepanel.js sends: -->
<!--   SPOILERSHIELD_SHOW_INFO    → VEIL_SHOW_INFO    -->
<!--   SPOILERSHIELD_CONTEXT      → VEIL_CONTEXT      -->
<!-- sidepanel.js listens for: -->
<!--   SPOILERSHIELD_REQUEST_SHOW_INFO    → VEIL_REQUEST_SHOW_INFO    -->
<!--   SPOILERSHIELD_REQUEST_CONTEXT      → VEIL_REQUEST_CONTEXT      -->
<!--   SPOILERSHIELD_REQUEST_REDETECT     → VEIL_REQUEST_REDETECT     -->

<!-- manifest.json current values: -->
<!--   "name": "SpoilerShield Side Panel"          → "Veil" -->
<!--   "description": "Opens SpoilerShield in..."  → "Ask anything about what you're watching, without spoilers." -->
<!--   "action.default_title": "Open SpoilerShield" → "Open Veil" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest.json — name, description, action title</name>
  <files>extension/manifest.json</files>
  <action>
Edit extension/manifest.json. Make exactly these changes:

1. `"name"`: change from `"SpoilerShield Side Panel"` to `"Veil"`
2. `"description"`: change from `"Opens SpoilerShield in a Chrome Side Panel and prefills recent subtitles as context."` to `"Ask anything about what you're watching — get answers without spoilers."`
3. `"action"."default_title"`: change from `"Open SpoilerShield"` to `"Open Veil"`

Do NOT change: manifest_version, version, permissions, host_permissions, icons paths, side_panel, content_scripts. Those are unchanged.

NOTE: The version field stays at "0.2.0" — version bump is deferred to Phase 3 (Web Store Launch, requirement STR-04).
  </action>
  <verify>
    <automated>node -e "const m = require('./extension/manifest.json'); console.assert(m.name === 'Veil', 'name wrong: ' + m.name); console.assert(m.action.default_title === 'Open Veil', 'action title wrong'); console.assert(!m.description.includes('SpoilerShield'), 'description still has SpoilerShield'); console.log('manifest OK')"</automated>
  </verify>
  <done>manifest.json name is "Veil", description and action title contain no SpoilerShield references.</done>
</task>

<task type="auto">
  <name>Task 2: Update content.js — chrome.storage keys and console prefix</name>
  <files>extension/content.js</files>
  <action>
Edit extension/content.js. Make exactly these changes:

1. Line 23 — console.log prefix: change `"[SpoilerShield]"` to `"[Veil]"`
   - The DEV_LOGGING guard means this only fires in dev; rename it anyway for consistency.

2. Line 295 — chrome.storage.local.set call: change the key from `spoilershield_show_info` to `veil_show_info`
   The object key inside the set call looks like: `{ spoilershield_show_info: showInfo }` → `{ veil_show_info: showInfo }`

3. Line 510 — chrome.storage.local.set call: change the key from `spoilershield_context` to `veil_context`
   The object key looks like: `{ spoilershield_context: record }` → `{ veil_context: record }`

Search for all occurrences of `spoilershield_` in content.js to make sure no others were missed. If any remain, rename them too.

Do NOT change: any logic, platform detection, subtitle buffer handling, DOM observation, or communication with background.js.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('./extension/content.js','utf8'); console.assert(!c.includes('spoilershield_'), 'spoilershield_ still present'); console.assert(c.includes('veil_show_info'), 'veil_show_info missing'); console.assert(c.includes('veil_context'), 'veil_context missing'); console.assert(!c.includes('[SpoilerShield]'), '[SpoilerShield] log still present'); console.log('content.js OK')"</automated>
  </verify>
  <done>content.js writes veil_show_info and veil_context to chrome.storage. No spoilershield_ strings remain. Console prefix is [Veil].</done>
</task>

<task type="auto">
  <name>Task 3: Update sidepanel.js — storage keys, postMessage types, console prefix</name>
  <files>extension/sidepanel.js, extension/app/index.html</files>
  <action>
Edit extension/sidepanel.js. Make ALL of these changes (every occurrence, not just the first):

**chrome.storage.local key reads and watches** (lines 34, 67, 86, 108, 111 — check all):
- `"spoilershield_show_info"` → `"veil_show_info"` (appears in get() calls, set() calls, and onChanged listener)
- `"spoilershield_context"` → `"veil_context"` (appears in get() calls, set() calls, and onChanged listener)
- `result.spoilershield_show_info` → `result.veil_show_info` (property access on the result object)
- `result.spoilershield_context` → `result.veil_context` (property access on the result object)

**postMessage types sent to React app**:
- `type: "SPOILERSHIELD_SHOW_INFO"` → `type: "VEIL_SHOW_INFO"`
- `type: "SPOILERSHIELD_CONTEXT"` → `type: "VEIL_CONTEXT"`

**postMessage types listened for from React app**:
- `"SPOILERSHIELD_REQUEST_SHOW_INFO"` → `"VEIL_REQUEST_SHOW_INFO"`
- `"SPOILERSHIELD_REQUEST_CONTEXT"` → `"VEIL_REQUEST_CONTEXT"`
- `"SPOILERSHIELD_REQUEST_REDETECT"` → `"VEIL_REQUEST_REDETECT"`

**console.log / console.error prefixes**:
- `'[SpoilerShield]'` → `'[Veil]'` (all occurrences)

After editing sidepanel.js, also edit extension/app/index.html:
- `<title>SpoilerShield - Ask Questions Without Spoilers</title>` → `<title>Veil - Ask Without Spoilers</title>`
- `og:title` meta content: change `"SpoilerShield - Spoiler-Safe Q&A for Shows & Anime"` → `"Veil - Spoiler-Safe Q&A for Shows & Anime"`
- `og:description` meta content: `"Ask questions about what you're watching. Get answers without spoilers."` → `"Ask anything about what you're watching — get answers without spoilers."`
- The `<meta name="description">` content: replace SpoilerShield with Veil.

Do a final search for `spoilershield` (case-insensitive) across both files and fix any remaining occurrences.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const s = fs.readFileSync('./extension/sidepanel.js','utf8'); console.assert(!s.includes('SPOILERSHIELD'), 'SPOILERSHIELD still in sidepanel.js'); console.assert(!s.includes('spoilershield'), 'spoilershield still in sidepanel.js'); console.assert(s.includes('VEIL_SHOW_INFO'), 'VEIL_SHOW_INFO missing'); console.assert(s.includes('veil_show_info'), 'veil_show_info missing'); const h = fs.readFileSync('./extension/app/index.html','utf8'); console.assert(!h.includes('SpoilerShield'), 'SpoilerShield in index.html'); console.assert(h.includes('Veil'), 'Veil missing from index.html'); console.log('sidepanel.js + index.html OK')"</automated>
  </verify>
  <done>sidepanel.js sends VEIL_SHOW_INFO / VEIL_CONTEXT, listens for VEIL_REQUEST_* types, reads veil_show_info / veil_context from chrome.storage. index.html title and meta tags say Veil. No spoilershield strings remain in either file.</done>
</task>

</tasks>

<verification>
Run the automated verifies for all three tasks sequentially. Then do a final grep across all modified extension files:

```bash
grep -rni "spoilershield" extension/manifest.json extension/content.js extension/sidepanel.js extension/app/index.html
```

Expected output: no matches.
</verification>

<success_criteria>
- manifest.json: name="Veil", description and action title are Veil-branded
- content.js: writes veil_show_info and veil_context to chrome.storage; [Veil] console prefix
- sidepanel.js: reads veil_* keys, sends VEIL_SHOW_INFO/VEIL_CONTEXT, responds to VEIL_REQUEST_* types; [Veil] console prefix
- extension/app/index.html: Veil in title and og:title
- Zero occurrences of "spoilershield" (case-insensitive) in any of these four files
</success_criteria>

<output>
After completion, create `.planning/phases/01-rebrand/01-01-SUMMARY.md` using the summary template.
</output>
